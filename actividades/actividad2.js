/*let estatura = parseFloat(prompt('Ingrese su estatura en cm' ));
let peso = parseInt(prompt('Ingrese su peso en kg'));
function calcularIMC(estatura,peso){
    let imc = peso/(estatura * estatura);
    console.log(imc);170
    return imc; 
}
calcularIMC(estatura,peso);*/

/*function calcularFactorial(numero) {
    if (numero === 0 || numero === 1) {
      return 1;
    } else {
      return numero * calcularFactorial(numero - 1);
    }
  }
  // Ejemplo de uso
  let numero = 5;
  let resultado = calcularFactorial(numero);
  console.log(`El factorial de ${numero} es ${resultado}`);*/



/*function convertidor(dolares){
    let tasadolar = 3.900;
    let pesos = dolares * tasadolar;
    console.log(`${dolares} dolares son ${pesos} pesos`);
    return pesos;
  }

let dolares = parseFloat(prompt('Ingrese la cantidad de dolares a convertir'))

convertidor(dolares );*/

/*function calcularAreaPerimetro(longitud,base){
    let area = longitud * base;
    let perimetro = 2 * (longitud + base);
    alert('El area de la sala rectangular es : ' + area);
    alert('El perimetro de la sala rectangular  es : ' + perimetro);
    return;
}
let longitud = parseFloat(prompt('Ingrese la longitud del rectangulo'));
let base = parseFloat(prompt('Ingrese la base del rectangulo'));

calcularAreaPerimetro(longitud, base);*/



/*function calcularAreaPerimetroCircular(radio){
    let pi = 3.1416;
    let area = pi * (radio * radio);
    let perimetro = 2 * (pi * radio);
    alert('El area de la sala circular es : ' + area);
    alert('El perimetro de la sala circular  es : ' + perimetro);
    return;
}
let radio = parseFloat(prompt('Ingrese el radio del circulo'));

calcularAreaPerimetroCircular(radio);*/

/*function tablaDeMultiplicar(){
    for (let i = 1; i <= 15; ){
        let resultado = numero * i;
        console.log(`${numero} x ${i} = ${resultado}`);
        i++;
    }
    return;

}

let numero = parseInt(prompt('Ingrese el número de la tabla de multiplicar'));
tablaDeMultiplicar(numero);*/